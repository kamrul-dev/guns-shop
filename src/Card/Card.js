import React from 'react';
import './Card.css'

const Card = (props) => {
        console.log(props);
    return (
        <div>
            <h1>this is card</h1>
        </div>
    );
};

export default Card;